<section class="ubicacion">

  <div class="mapa">
    <img src="/mapa.png" alt="Mapa ubicación">
    <div class="overlay"></div>
  </div>

  <div class="info">
    <h2>Encuéntranos en el mapa</h2>
    <p>
      Nuestra casa central está en Saladillo, pero hacemos negocios inmobiliarios en toda la provincia de Buenos Aires. Va a encontrar nuestros carteles de venta o alquiler en Saladillo y alrededores, Roque Pérez, 25 de Mayo, toda la Costa Atlántica, La Plata, Las Flores, General Alvear y más.

Si está planeando invertir en lotes, casas, chacras o campos, va a encontrar con nuestra firma un servicio de plena confianza con los mejores espacios de cada ciudad a su disposición.

    </p>
    <a class="btn">Estamos en Av. Belgrano 2498, Saladillo, Provincia de Buenos Aires. Puede encontrarnos en nuestras oficinas de lunes a viernes de 9 a 12hs, y de 17 a 19hs.</a>
  </div>

</section>
<style>
    .ubicacion {
  display: grid;
  grid-template-columns: 1.2fr 1fr;
  min-height: 400px;
}

/* MAPA */
.mapa {
  position: relative;
  overflow: hidden;
}

.mapa img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  transition: transform 0.5s ease;
}

/* overlay oscuro elegante */
.overlay {
  position: absolute;
  inset: 0;
}

/* efecto hover */
.mapa:hover img {
  transform: scale(1.05);
}

/* INFO */
.info {
  background: linear-gradient(135deg, #0d4d8b, #0a2e5c);
  color: white;
  display: flex;
  flex-direction: column;
  justify-content: center;
  padding: 60px;
}

.info h2 {
  font-size: 2rem;
  margin-bottom: 15px;
}

.info p {
  opacity: 0.85;
  line-height: 1.6;
  margin-bottom: 25px;
}

/* BOTÓN */
.btn {
  display: inline-block;
  padding: 12px 25px;
  background: white;
  color: #0d4d8b;
  font-weight: bold;
  text-decoration: none;
  border-radius: 8px;
  transition: 0.3s;
}

.btn:hover {
  background: #eaeaea;
  transform: translateY(-2px);
}
@media (max-width: 768px) {
  .ubicacion {
    grid-template-columns: 1fr;
  }

    .mapa img{
       display: none;
    }

  .info {
    padding: 30px;
    text-align: center;
  }
}
</style>