<section class="contacto" aria-labelledby="contacto-titulo">
  <div class="contacto-inner">
    <div class="contacto-box">
      <h2 id="contacto-titulo">
        <span>Contacto</span>
        Contactanos
      </h2>
      <p class="contacto-lead">Dejanos tus datos y te respondemos a la brevedad</p>

      <form class="form">
        <input type="text" name="nombre" placeholder="Nombre completo" required autocomplete="name" />
        <input type="tel" name="telefono" placeholder="Número de teléfono" required autocomplete="tel" />
        <input type="email" name="email" placeholder="Correo electrónico" required autocomplete="email" />
        <textarea name="mensaje" placeholder="Escribí tu consulta..." rows="4" required></textarea>
        <button type="submit">Enviar consulta</button>
      </form>
    </div>
  </div>
</section>

<style>
  .contacto {
    position: relative;
    overflow: hidden;
    display: flex;
    justify-content: center;
    align-items: center;
    padding: clamp(3rem, 8vw, 5rem) 20px;
    min-height: min(72vh, 720px);
  }

  .contacto::before {
    content: "";
    position: absolute;
    inset: 0;
    background: linear-gradient(
      165deg,
      rgba(13, 77, 139, 0.97) 0%,
      rgba(10, 46, 92, 0.95) 38%,
      rgba(8, 35, 70, 0.98) 100%
    );
    z-index: 0;
  }

  .contacto::after {
    content: "";
    position: absolute;
    inset: 0;
    background: radial-gradient(
      ellipse 80% 60% at 50% 20%,
      rgba(59, 130, 246, 0.18) 0%,
      transparent 55%
    );
    z-index: 1;
    pointer-events: none;
  }

  .contacto-inner {
    position: relative;
    z-index: 2;
    width: 100%;
    max-width: 520px;
    margin: 0 auto;
  }

  .contacto-box {
    padding: clamp(1.75rem, 4vw, 2.5rem) clamp(1.5rem, 4vw, 2.25rem);
    border-radius: 20px;
    background: rgba(15, 35, 70, 0.35);
    backdrop-filter: blur(12px);
    -webkit-backdrop-filter: blur(12px);
    border: 1px solid rgba(147, 197, 253, 0.22);
    box-shadow:
      0 24px 48px rgba(8, 30, 70, 0.35),
      inset 0 1px 0 rgba(255, 255, 255, 0.06);
    color: #fff;
    font-family: "Poppins", sans-serif;
  }

  .contacto-box h2 {
    font-size: clamp(1.75rem, 4vw, 2.25rem);
    font-weight: 600;
    color: #f0f7ff;
    line-height: 1.28;
    margin: 0 0 0.75rem;
    text-shadow: 0 2px 24px rgba(8, 25, 60, 0.45);
  }

  .contacto-box h2 span {
    display: block;
    font-size: clamp(0.8125rem, 2vw, 0.9375rem);
    color: #bfdbfe;
    font-weight: 500;
    margin-bottom: 0.65rem;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    text-shadow: 0 1px 12px rgba(8, 30, 70, 0.4);
  }

  .contacto-lead {
    margin: 0 0 1.5rem;
    font-size: 0.9375rem;
    line-height: 1.55;
    opacity: 0.88;
  }

  .form {
    display: flex;
    flex-direction: column;
    gap: 0.9rem;
  }

  .form input,
  .form textarea {
    width: 100%;
    box-sizing: border-box;
    padding: 14px 16px;
    border-radius: 12px;
    border: 1px solid rgba(255, 255, 255, 0.12);
    background: rgba(255, 255, 255, 0.95);
    color: #0f172a;
    font-family: "Poppins", sans-serif;
    font-size: 0.9375rem;
    outline: none;
    resize: vertical;
    transition: border-color 0.2s ease, box-shadow 0.2s ease;
  }

  .form input::placeholder,
  .form textarea::placeholder {
    color: #64748b;
  }

  .form input:focus,
  .form textarea:focus {
    border-color: rgba(147, 197, 253, 0.65);
    box-shadow: 0 0 0 3px rgba(59, 130, 246, 0.2);
  }

  .form button {
    margin-top: 0.35rem;
    padding: 0.85rem 1.75rem;
    border: 1px solid rgba(191, 219, 254, 0.35);
    border-radius: 999px;
    background: linear-gradient(135deg, #2563eb 0%, #3b82f6 45%, #60a5fa 100%);
    color: #fff;
    font-family: "Poppins", sans-serif;
    font-size: 0.875rem;
    font-weight: 500;
    letter-spacing: 0.06em;
    cursor: pointer;
    transition: transform 0.25s ease, box-shadow 0.25s ease;
  }

  .form button:hover {
    transform: translateY(-2px);
    box-shadow:
      0 12px 32px rgba(37, 99, 235, 0.45),
      0 0 0 1px rgba(147, 197, 253, 0.2);
  }

  .form button:focus-visible {
    outline: 2px solid #bfdbfe;
    outline-offset: 3px;
  }

  @media (max-width: 768px) {
    .contacto {
      min-height: unset;
      padding: 3rem 16px;
    }

    .contacto-box {
      border-radius: 16px;
    }
  }
</style>
